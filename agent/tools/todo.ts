import { disableTool } from "eve/tools";

// "Three complete visions are coming right up… One sec" — then the planner
// wrote itself a to-do list and went silent; a load-test brief (Austin,
// wrun_41M1C80F110GXWHXCV00BP7TGZ) ended exactly there at minute 12. The
// measurement that removed the scout's list applies unchanged here: a round
// trip spent on bookkeeping is 10-30 seconds the couple waits, and the list
// records intent the same turn should have executed instead.
export default disableTool();
